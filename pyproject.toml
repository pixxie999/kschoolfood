[project]
name = "kschoolfood"
version = "0.1.0"
description = "Global K-School Food WebApp using Cloudflare Workers & D1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2",
    "httpx",
    "markdown"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
